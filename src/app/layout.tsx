import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "NORTH ↑ | Find your direction.",
  description: "AI Skill Matcher + Decision Assistant. Map your skills, surface income paths, and cut through the noise.",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" className={`${geistSans.variable} ${geistMono.variable} h-full`}>
      <body className="bg-background text-text-primary font-sans h-full overflow-x-hidden selection:bg-accent/30">
        {children}
      </body>
    </html>
  );
}
